<div class='row text-center'>
  <h1>Already checked sites</h1>
</div>
<div class='row'>
<% @sites.each do |site| %>
  <div class= col-lg-10>
    <h3><%=site.url%></h3>
<p> <%= link_to "View Links", site_path(site)%></br>
<a href=<%=site.url%>> Visit Site </a> </p>
</div>
<% end %>

</div>

